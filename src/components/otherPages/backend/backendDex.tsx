import * as React from 'react';

export interface IBackendPageProps {
}

export interface IBackendPageState {
}

export default class BackendPage extends React.Component<IBackendPageProps, IBackendPageState> {
  constructor(props: IBackendPageProps) {
    super(props);

    this.state = {
    }
  }

  public render() {
    return (
      <div>
        
      </div>
    );
  }
}
